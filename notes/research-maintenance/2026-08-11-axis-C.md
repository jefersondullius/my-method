# Axis C — Skills and agents · maintenance run, 2026-08-11

Window: lower bound **2026-08-10** (axis C's last check, carried over from
`notes/audit-02-skills.md`; `notes/maintenance/LAST-CHECK.md:11` records it as
"not run" since). Research only — nothing installed, no setting changed, no file
modified except this one.

Every external claim below carries a URL and "accessed 2026-08-11". Every repo
claim carries `file:line`. Prior knowledge was not used as a source. HTTP 200 was
never treated as evidence: the marketplace catalog was downloaded and parsed as
JSON, not summarized, after a summarizing fetch of the same URL returned a
**wrong plugin count** (see Task 3, first note).

---

## Task 1 — everything installed

### 1.1 Raw output — `claude plugin list --json`

Run from `C:\Users\Jeferson\Documents\dev\my-method`, 2026-08-11:

```json
[
  {
    "id": "my-method@jeferson-tools",
    "version": "0.1.0",
    "scope": "user",
    "enabled": true,
    "installPath": "C:\\Users\\Jeferson\\.claude\\plugins\\cache\\jeferson-tools\\my-method\\0.1.0",
    "installedAt": "2026-08-10T05:17:26.209Z",
    "lastUpdated": "2026-08-10T05:17:26.209Z"
  },
  {
    "id": "my-method@skills-dir",
    "version": "unknown",
    "scope": "user",
    "enabled": false,
    "installPath": "",
    "errors": [
      "Not loaded — the name \"my-method\" is already taken by an installed plugin (my-method@jeferson-tools), which takes precedence. Give the plugin at .\\.claude\\skills\\my-method a different \"name\" (in plugin.json or SKILL.md frontmatter) to load this copy."
    ]
  }
]
```

**Watchlist recipe check (Axis A row "CLI: `claude plugin list --json`"):** the
recipe says confirm `scope`, `enabled`, `installPath`, `errors` are still in the
real output. **All four are still present**, on 2026-08-11, with the CLI at the
version noted in 1.5. The docs still describe fewer fields than the command
emits — unchanged since the last check.

### 1.2 Raw output — `claude plugin marketplace list`

```
Configured marketplaces:

  ❯ claude-plugins-official
    Source: GitHub (anthropics/claude-plugins-official)

  ❯ jeferson-tools
    Source: Directory (C:\Users\Jeferson\Documents\dev\my-method)
```

Two marketplaces, both alive. Note the asymmetry worth recording: `plugin list`
reports a plugin sourced from **`skills-dir`**, which is *not* one of the two
configured marketplaces — `skills-dir` is the harness's internal label for a
plugin discovered under `.claude/skills/`, not a marketplace you can add or
remove. Nothing to fix; recorded so a future run does not go looking for a third
marketplace.

### 1.3 Per-plugin verdict — `my-method@jeferson-tools`

| Question | Answer | Evidence |
|---|---|---|
| Current version | **0.2.0** | `kit/my-method/.claude-plugin/plugin.json` (`"version": "0.2.0"`); `claude plugin details my-method` prints `my-method 0.2.0` |
| Version the install record reports | **0.1.0** | raw output above; cached manifest at `…\cache\jeferson-tools\my-method\0.1.0\.claude-plugin\plugin.json` still says `"version": "0.1.0"` |
| Renamed? | **No** | name `my-method` in `.claude-plugin/marketplace.json` and in `kit/my-method/.claude-plugin/plugin.json`; `git log -- .claude-plugin/marketplace.json` shows one commit (`19b4bc6`), no rename |
| Deprecated / abandoned? | **No** | authored in this repo; `git log --oneline -12` shows commits through 2026-08-11 (`2a9d5d4`) |
| Manifest valid? | **Yes** | `claude plugin validate ./kit/my-method` → `✔ Validation passed` (watchlist Axis A recipe satisfied) |

**Live source note.** This plugin is not published anywhere external — the only
live source for "current version / renamed / abandoned" is this repository plus
the harness's own probes. Both were used.

**The 0.1.0 ≠ 0.2.0 gap is expected, and already documented.**
`notes/maintenance/LAST-CHECK.md:90-95` and `CHANGELOG.md:51-55` both record it:
the install record stays pinned to the version installed on 2026-08-10, while
`plugin details` reads the kit source live. Confirmed still true today. This run
adds **physical corroboration that was not in the record**: the pinned cache
directory does not merely carry an old version number, it is **missing most of
the kit**. `diff -rq` between `…\cache\jeferson-tools\my-method\0.1.0` and
`kit/my-method`:

```
Only in kit/my-method: agents          (security-reviewer.md)
Only in kit/my-method: hooks           (hooks.json, verify-gate.ps1)
Only in kit/my-method/commands: friction.md, health-check.md,
                                start-project.md, update-method.md, where-am-i.md
Files …/commands/next-task.md and kit/my-method/commands/next-task.md differ
Files …/templates/{CLAUDE,STATE,TASK-XXX}.md and kit/… differ
```

The cache holds **1 command, 4 templates, no agent, no hook**. Yet
`claude plugin details my-method` reports, today:

```
Component inventory
  Skills (6)  friction, health-check, next-task, start-project, update-method, where-am-i
  Agents (1)  security-reviewer
  Hooks (1)  PreToolUse  (harness-only — no model context cost)
```

So the commit gate and the `security-reviewer` that this method depends on are
loaded **only** because the harness reads the kit source live. That is the T4
conclusion (`LAST-CHECK.md:73-98`) confirmed by a second, independent method: T4
proved it by adding a file, this run proves it by showing the frozen cache
physically lacks the components the harness reports. It remains **undocumented
behaviour** — no Anthropic page describes it — so the exposure `LAST-CHECK.md:96`
names is real and unchanged. Probe 1 of `/health-check` comparing component
inventory rather than version strings is the correct design and needs no change.

### 1.4 Per-plugin verdict — `my-method@skills-dir`

| Question | Answer | Evidence |
|---|---|---|
| Current version | **n/a** — reports `"unknown"`, `enabled: false` | raw output above |
| What it actually is | A **symlink**, not a second copy: `.claude/skills/my-method -> /c/Users/Jeferson/Documents/dev/my-method/kit/my-method` | `ls -la .claude/skills/my-method` |
| Renamed / deprecated / abandoned? | **Not applicable** — same source tree as the entry above | same |
| Action | **None needed; do not delete casually** | see below |

This is finding **F4** from `friction.md:236-237` ("sobrou uma junção
`.claude/skills/my-method` da era pré-marketplace, sombreada pela instalação e
não carregada"), still present and still harmless. New detail this run adds: it
is a symlink into `kit/my-method`, so its content can never drift from the
installed plugin — the error is a name collision, not a stale duplicate.
`CHANGELOG.md:541` records it was created during a pilot on purpose. Removing it
is a user decision, not a maintenance defect; leaving it costs one line of
`errors` in probe 1's output, which probe 1 already reports (`CHANGELOG.md:23`).

### 1.5 Incidental — the CLI itself moved during the last session

`CHANGELOG.md:57-62` records Claude Code self-updating mid-session on 2026-08-11
(2.1.227 → 2.1.228), which made one `claude plugin validate` fail and pass on
retry. All three CLI probes above were re-run today and all three behaved as the
watchlist recipes expect, so nothing in axis C's tooling broke across that bump.

---

## Task 2 — the six DEFERRED items, re-read against their recorded triggers

Files read in full for this task: `friction.md` (261 lines, both sections),
`notes/maintenance/LAST-CHECK.md`, `notes/audit-02-skills.md` §§ "Recommendation"
and "skill-creator", plus targeted reads of `CHANGELOG.md`, `method.md`,
`playbook/SECURITY-MATRIX.md`.

**One fact governs three of the six verdicts:** this repository contains **no
product `PLAN.md`**. `find . -iname "*PLAN*"` returns exactly one path —
`kit/my-method/templates/PLAN.md`, a template shipped by the kit. There is no
plan of a real project here to observe a task on.

**A second fact governs two more:** `friction.md`'s **`MINE` section is empty**
— the file ends at line 261 with the `## MINE` heading and nothing under it. The
user has logged zero complaints of their own to date. Any trigger phrased "a
friction entry says so" therefore cannot have fired, in this repository.

---

### C1 · `frontend-design` — UNKNOWABLE-FROM-HERE

**Trigger, verbatim:** "First user-facing-screens task on a real project's plan."

**Evidence checked**
- No product plan exists in this repo: only `kit/my-method/templates/PLAN.md`.
- This repository is the method kit itself — it builds no user-facing screens.
- `notes/audit-02-skills.md:218` states the trigger identically; nothing in the
  repo has changed that would satisfy it.

**Verdict: UNKNOWABLE-FROM-HERE.** The trigger is a fact about the user's *other*
projects. Watchlist column "Observable from this repo? **No**" is correct and
should stay.

**Exact question for the main session to ask the user:**
> Desde 2026-08-10, algum projeto seu (fora deste repositório) ganhou no
> `PLAN.md` uma tarefa de **telas que o usuário final vê**? Se sim, qual projeto
> e qual tarefa? (É o gatilho registrado para instalar `frontend-design`.)

**Live status of the target, if the answer is yes:** still exists, still
Anthropic's, still under the exact name — see Task 3.

---

### C2 · `webapp-testing` — UNKNOWABLE-FROM-HERE (the observable half is NOT MET)

**Trigger, verbatim:** "A web-UI task reaches verification AND committed
Playwright specs prove hard to author (a friction entry says so)."

**Evidence checked**
- Half 1 — a web-UI task reaching verification: no product plan here (above);
  the only end-to-end pilot on record built a **PowerShell + WinForms desktop
  app**, not a web UI (`CHANGELOG.md:229-230`), and its two automated checks were
  `check-syntax.ps1` and `check-roundtrip.ps1` (`CHANGELOG.md:249-251`).
- Half 2 — a friction entry saying specs are hard to author: `friction.md`
  `MINE` is empty (line 261); the seven `YOURS` entries (lines 5-258) are about
  plugin mechanics, `${CLAUDE_PLUGIN_ROOT}`, provenance of v4/v5/v6/v7 and the
  maintenance proposal — none mentions Playwright, specs, or web UI.
- No Playwright artifact of any kind exists in the repo: `find` for
  `*.spec.*` / `playwright*` returns nothing.

**Verdict: UNKNOWABLE-FROM-HERE for the user's other projects; NOT MET for
everything observable here.** Note audit-02's ordering constraint, still binding:
"Committed specs come first" (`notes/audit-02-skills.md:220`) — the trigger cannot
fire before specs exist and prove painful.

**Exact question:**
> Em algum projeto seu, uma tarefa de **interface web** chegou à verificação
> desde 2026-08-10 — e, se chegou, você registrou no `friction.md` daquele
> projeto que escrever os testes Playwright commitados foi difícil? (São as duas
> metades do gatilho; a segunda exige uma entrada de friction, não uma impressão.)

**Live status of the target (re-verified today).** `webapp-testing` lives in
`anthropics/skills` at `skills/webapp-testing/` — `SKILL.md` (3913 bytes),
`LICENSE.txt`, `examples/`, `scripts/`
[https://api.github.com/repos/anthropics/skills/contents/skills/webapp-testing —
accessed 2026-08-11]. Frontmatter `name: webapp-testing` unchanged; description:
"Toolkit for interacting with and testing local web applications using
Playwright…"
[https://raw.githubusercontent.com/anthropics/skills/main/skills/webapp-testing/SKILL.md
— accessed 2026-08-11]. Repo not archived, not disabled, `pushed_at`
2026-08-07T17:14:15Z
[https://api.github.com/repos/anthropics/skills — accessed 2026-08-11]. Last
commit touching that path: `b9e19e6f`, 2026-04-20 (a LICENSE copyright fill);
before it, `ef740771`, 2025-12-01 (folder restructure) — so the skill's substance
has been stable for ~8 months. **Not renamed, not deprecated, not abandoned.**
Audit-02's "manual copy, not the bundle" is re-confirmed: `webapp-testing` is
**absent from the official marketplace's 287 plugins** (Task 3).

---

### C3 · `playwright` MCP plugin — UNKNOWABLE-FROM-HERE

**Trigger, verbatim:** "Same family as above, only if interactive browser
debugging is needed."

**Evidence checked** — identical to C2: no web-UI work, no browser debugging need
anywhere in this repo. The trigger is strictly downstream of C2's.

**Verdict: UNKNOWABLE-FROM-HERE.** Watchlist column "**No**" is correct.

**Exact question** (ask only if C2's answer is yes):
> Nesse trabalho de interface web, você precisou de **depuração interativa no
> navegador** — abrir a página, clicar, ler o console ao vivo — ou os testes
> Playwright escritos e commitados bastaram?

**Live status:** exists, Microsoft's, exact name — see Task 3. Caveat recorded
there: it is the **least-recently-touched** of the five (2025-12-02).

---

### C4 · `security-guidance` — NOT MET here; the deciding half is UNKNOWABLE-FROM-HERE

**Trigger, verbatim:** "Matrix wiring done (it is, since v5) AND a project
triages into a login/personal-data tier. Try Semgrep-in-verify + built-in
`/security-review` first."

**Evidence checked — half 1 (wiring): MET, confirmed today.**
- `method.md:1` is v7; the security rows are wired into STEP 5c by kind —
  drift re-triage, AUTOMATED rows as commands, REVIEW rows to one fresh
  `security-reviewer`, HUMAN DECISION rows verbatim to the user
  (`method.md:157-176`).
- The matrix defines the tiers, T2 being "Requires login; stores data tied to a
  specific person" (`playbook/SECURITY-MATRIX.md:55`).

**Evidence checked — half 2 (a project in a login/personal-data tier): NOT MET.**
- The only completed pilot triaged **T0**: "Security tier: T0 — razão" with
  "Security: none applicable" justified line by line
  (`CHANGELOG.md:239-244`).
- That pilot's declared limit 1: REVIEW rows did not fire — correctly, T0 has no
  applicable row — "isso significa que o caminho revisor-em-fluxo segue não
  exercitado; exige um piloto T2 (login + dado pessoal)" (`CHANGELOG.md:261-263`).
- The T2 pilot is **still pending** and has accumulated four separate validation
  debts (`CHANGELOG.md:172-177`).
- Nothing between 2026-08-10 and today changed this: the most recent CHANGELOG
  entry (`CHANGELOG.md:3-62`) is the maintenance-command tests T1–T5/T8, which
  are kit tooling, not a product triage.

**Evidence checked — the "try first" clause: still untried, and one prerequisite
is missing.** Semgrep-in-verify cannot run on this machine yet: `semgrep`,
`gitleaks` and `pip-audit` are **absent** (`friction.md:227-228`, re-confirmed by
health-check T1, `CHANGELOG.md:15-16`). The built-in `/security-review` is
available at zero install (`notes/audit-02-skills.md:192-194`).

**Verdict: NOT MET.** Half 1 MET, half 2 NOT MET, and the trigger is a
conjunction. Additionally the deterministic-first ladder has not been climbed at
all, so even a T2 project would not reach this plugin on its first step.

**One forward-looking fact worth surfacing:** `playbook/SECURITY-MATRIX.md:55`
states, of tier T2, "**This is the shape of the next project.**" The repo
therefore *expects* half 2 to fire soon — but expectation is not observation, and
no such project exists yet.

**Exact question:**
> Você já começou (ou vai começar agora) um projeto que **exige login** ou que
> **guarda dado pessoal de terceiros** — ou seja, T2 na matriz? Se sim: antes de
> instalar `security-guidance`, a ordem registrada manda tentar primeiro Semgrep
> dentro do `verify.ps1` e o `/security-review` embutido. Quer que eu prepare
> essa ordem? (E aviso: `semgrep` não está instalado nesta máquina.)

**Live status:** exists, exact name, **version 2.0.6**, actively maintained —
see Task 3. It is the only one of the five that publishes a version number.

---

### C5 · `skill-creator` — NOT MET (this is the one the watchlist says is fully observable here)

**Trigger, verbatim:** "A scheduled kit task that authors or tunes a skill with
evals."

**Evidence checked**
- **No scheduled task of any kind exists on a plan in this repo** — there is no
  product `PLAN.md`, only the template. Kit work is scheduled by user request and
  written to `notes/proposals/`, not to a plan file.
- **No eval mechanism exists anywhere in the repo.** A case-insensitive search for
  `\beval|evals` across all files returns only: the English word
  "evaluate/evaluation" in prose (`method.md:25,81,88`,
  `kit/my-method/commands/start-project.md:59,115,122,592,602`,
  `kit/my-method/commands/friction.md:2,27`,
  `kit/my-method/agents/security-reviewer.md:20`), the audits' own use of the word
  (`notes/audit-01-gaps.md:5`, `notes/audit-02-skills.md`, `notes/audit-03-agents.md`
  incl. the evaluator-optimizer pattern at `:192,217,229,286`), and **descriptions
  of `skill-creator` itself** in the research notes
  (`notes/research-skills/skill-creator.md`, `notes/research-skills/discovery.md:32`).
  No eval harness, no eval runner, no benchmark, no scheduled eval.
- **Two kit skills were in fact authored inside the window** — `health-check.md`
  and `update-method.md`, applied 2026-08-11 (`CHANGELOG.md:64-98`). They were
  **not** authored with evals: they were validated by six live headless probes,
  T1–T5 and T8 (`CHANGELOG.md:3-43`). That approach worked — T2 caught the exact
  failure mode this class of command is prone to ("sonda que 'passa' porque nada a
  negou" → reported INCONCLUSIVE, `CHANGELOG.md:17-20`) and T8 produced an honest
  negative with two independent detectors (`CHANGELOG.md:38-43`).
- `notes/maintenance/LAST-CHECK.md` schedules **axes**, not skill-authoring
  tasks, and lists no eval work.

**Verdict: NOT MET.** No scheduled task; no evals; the skill-authoring that did
happen was validated by live probes instead, and those probes did their job.

**The honest near-miss, recorded so it is not lost.** The same session produced a
documented *measurement* failure of exactly the kind `skill-creator`'s
benchmarking addresses: the proposal estimated "~100 tokens a mais por sessão"
and the real figure measured by `claude plugin details` was **+186, nearly double**
(`CHANGELOG.md:47-50`). That is a cost-estimate error, not a trigger-accuracy or
A/B failure — the two things audit-02 says the harness genuinely lacks
(`notes/audit-02-skills.md:156-158`) — and it was caught for free by a command the
kit already has. So it strengthens rather than weakens the DEFER: the cheap probe
found the error. **No question for the user is needed for this item** — it is
answerable from here, and the answer is no.

---

### C6 · TDD (staged, prose-only, no install) — NOT MET, with one judgement call named

**Trigger, verbatim:** "First observed 'check passed but behaviour wrong', or an
unfalsifiable check, in a pilot."

**Evidence checked — every pilot record in the repo.**
1. **`method.md:191-193`, note 5c** — in a pilot, "a checagem visual foi rotulada
   'humana' sem tentar automatizar, e só foi tentada depois que o usuário pediu a
   saída bruta." A check **skipped and mislabelled**, not a check that passed
   while behaviour was wrong. Already closed by v6's gate.
2. **`method.md:193-198`, note 5d** — two pilot failures: `PLAN.md` left "pending"
   while `STATE.md` said "done", and the end-of-turn text not emitted. Status-file
   desync, not a false-passing check. Closed by v6.
3. **`CHANGELOG.md:222-257`, the v5+v6 end-to-end pilot** — PASSED. The two
   automated checks genuinely ran, exit 0, evidence committed with `pass: true`
   and the parent commit as `head` (`CHANGELOG.md:249-254`). No wrong behaviour
   observed.
4. **`CHANGELOG.md:265-267`, that pilot's declared limit 2** — *this is the only
   candidate*: "A checagem HUMANA foi confirmada pela persona simulada, não por um
   humano clicando — o app não foi testado em GUI de verdade (as duas checagens
   automatizadas rodaram de verdade, exit 0)."
5. **`friction.md:130-133`** — the honest caveat carried by the v6 proposal: the
   gate proves verification "RODOU e PASSOU há pouco; se as checagens são as
   CERTAS continua sendo julgamento (L3)". A declared limitation, not an
   observation.
6. **`CHANGELOG.md:17-20`, test T2** — an unfalsifiable-check risk was found and
   closed *before* it could fire, and it was in a **test of the kit**, not a
   product pilot.
7. **`friction.md` `MINE` is empty** — the user has reported no such experience.

**Verdict: NOT MET**, on the strict reading of both disjuncts. There is no pilot
record in which a check passed and the behaviour was wrong, and no check that ran
and could not have failed. Item 4 is the one judgement call: a human check whose
pass was asserted by the same simulated persona that was driving the session,
with the GUI never exercised, is a check that could not have failed **within that
harness**. Against counting it: the pilot itself declares it as a limitation of
the *test rig* (headless, simulated owner), the two automated checks did really
run, and no wrong behaviour was ever observed. I am not deciding this for the
user.

**Exact question (only for the judgement call):**
> No piloto de 2026-08-11, a checagem "humana" foi dada como aprovada por uma
> persona simulada, sem ninguém abrir a GUI (`CHANGELOG.md:265-267`). Você quer
> contar isso como "checagem infalsificável em piloto" — o que dispararia o
> estágio TDD (só prosa, nada instalado) — ou tratar como limitação do arnês de
> teste e esperar um caso real? Minha leitura estrita: **não conta**, porque
> nenhum comportamento errado foi observado.

---

### Task 2 summary

| # | Item | Verdict | Decided from |
|---|---|---|---|
| C1 | `frontend-design` | **UNKNOWABLE-FROM-HERE** | no product plan in this repo |
| C2 | `webapp-testing` | **UNKNOWABLE-FROM-HERE** (observable half NOT MET) | no web UI, no specs, `MINE` empty |
| C3 | `playwright` MCP | **UNKNOWABLE-FROM-HERE** | downstream of C2 |
| C4 | `security-guidance` | **NOT MET** (wiring MET, tier NOT MET) | only pilot was T0; T2 pilot pending |
| C5 | `skill-creator` | **NOT MET** — answerable here, no question needed | no scheduled task, no evals anywhere |
| C6 | TDD (prose-only) | **NOT MET**, one judgement call for the user | no false-pass in any pilot record |

**Nothing to install. The empty set still holds** — `notes/audit-02-skills.md:206`
("Minimum set to install now: NOTHING") survives this re-check unchanged.

---

## Task 3 — the official marketplace, re-checked for renames

**Method, and why it matters.** `WebFetch` on the catalog URL — which summarizes
through a small model — reported **221 plugins** and claimed `playwright`,
`security-guidance` and `skill-creator` **do not exist**. All four claims are
**wrong**. The file was then downloaded raw (169,544 bytes, HTTP 200) and parsed
with `json.load`. Every number below comes from the parse. **Recipe correction for
the watchlist: this file must be parsed, never summarized.**

Source:
`https://raw.githubusercontent.com/anthropics/claude-plugins-official/main/.claude-plugin/marketplace.json`
— accessed 2026-08-11.

### 3.1 Catalog size

- Top-level keys: `$schema`, `name`, `description`, `owner`, `renames`, `plugins`.
- **`len(plugins) = 287`.**
- Baseline for the delta: `notes/audit-02-skills.md:183` counted **285** on
  2026-08-10. **+2 in one day.** The catalog is actively changing: the two most
  recent commits touching `marketplace.json` are `5ae6abd4` and `141b79a4`, both
  2026-08-11T22:xx
  [https://api.github.com/repos/anthropics/claude-plugins-official/commits?path=.claude-plugin/marketplace.json
  — accessed 2026-08-11].
- Repo alive: not archived, not disabled, `pushed_at` 2026-08-11T22:08:10Z
  [https://api.github.com/repos/anthropics/claude-plugins-official — accessed
  2026-08-11].

### 3.2 The `renames` map — full contents, verbatim

Nine entries, `old → new`:

```json
{
  "adlc": "agentforce-adlc",
  "airwallex": "airwallex-agentos",
  "convex-backend": "convex",
  "vals": "valtown",
  "wordpress.com": "build-with-wordpress",
  "qodo-skills": "qodo",
  "atlassian-forge-skills": "forge-skills",
  "azure-skills": "azure",
  "sonarqube-agent-plugins": "sonarqube"
}
```

**None of the five plugins on this axis appears in the map — neither as a key nor
as a value.** Checked both directions programmatically for `frontend-design`,
`webapp-testing`, `playwright`, `security-guidance`, `skill-creator`: zero hits.
No rename to absorb.

### 3.3 Per-plugin status in the catalog

| Name queried | Present under that exact name | Version | Source path | Author | Last commit to its dir |
|---|---|---|---|---|---|
| `frontend-design` | **YES** | *no version field* | `./plugins/frontend-design` | Anthropic | `d029127f` 2026-06-15 "Update frontend-design skill (#2540)" |
| `webapp-testing` | **NO — absent from all 287** | — | — | — | n/a (lives in `anthropics/skills`, see C2) |
| `playwright` | **YES** | *no version field* | `./external_plugins/playwright` | Microsoft | `22d3def3` 2025-12-02 |
| `security-guidance` | **YES** | **2.0.6** | `./plugins/security-guidance` | Anthropic (David Dworken) | `4e56d19d` 2026-06-12 |
| `skill-creator` | **YES** | *no version field* | `./plugins/skill-creator` | Anthropic | `2a40fd2e` 2026-04-23 "sync from anthropics/skills" |

Descriptions, verbatim from the catalog:

- **frontend-design** — "Create distinctive, production-grade frontend interfaces
  with high design quality. Generates creative, polished code that avoids generic
  AI aesthetics." (Its own `plugin.json` carries a shorter, different string:
  "Frontend design skill for UI/UX implementation".)
- **playwright** — "Browser automation and end-to-end testing MCP server by
  Microsoft. Enables Claude to interact with web pages, take screenshots, fill
  forms, click elements, and perform automated browser testing workflows."
- **security-guidance** — "Security review for Claude-generated code.
  Pattern-based warnings on edits, LLM-powered diff review on Stop, and an agentic
  commit reviewer that catches injection, XSS, SSRF, hardcoded secrets, and 25+
  other vulnerability classes."
- **skill-creator** — "Create new skills, improve existing skills, and measure
  skill performance. Use when users want to create a skill from scratch, update or
  optimize an existing skill, run evals to test a skill, or benchmark skill
  performance with variance analysis."

Each plugin's own `.claude-plugin/plugin.json` was fetched separately (all HTTP
200, all with content). Only `security-guidance` declares a `version` — and that
is **normal, not a defect**: **only 14 of 287 catalog entries carry a `version`
field at all**. A missing version is therefore not an abandonment signal in this
catalog, and a future run must not read it as one.

**Verdict per plugin: none renamed, none deprecated, none abandoned.** The two
weakest maintenance signals, recorded honestly: `playwright` has not been touched
since **2025-12-02** (it is a thin wrapper around Microsoft's MCP server, so this
is expected but unverified), and `skill-creator` since **2026-04-23** — matching
audit-02's "last substantive change 2026-03/04" (`notes/audit-02-skills.md:154`),
i.e. it has moved once since that audit, by a sync from `anthropics/skills`.

### 3.4 New finding — a stale repo name inside the catalog

The `homepage` fields of **`frontend-design`** and **`playwright`** point at
`https://github.com/anthropics/claude-plugins-**public**/tree/main/...`, while
`security-guidance` and `skill-creator` point at `claude-plugins-**official**`.

`anthropics/claude-plugins-public` returns **HTTP 301 Moved Permanently** and
resolves to `anthropics/claude-plugins-official`
[https://api.github.com/repos/anthropics/claude-plugins-public — accessed
2026-08-11].

So the **repository** was renamed public → official at some point, and two catalog
entries still carry the old name in their `homepage`. This is **not** a plugin
rename and breaks nothing today — GitHub's redirect is permanent and transparent.
It is recorded because it is exactly the class of drift the watchlist exists to
catch, and because a future run that hard-codes `claude-plugins-public` into a
recipe would be building on a redirect.

---

## NOT VERIFIED — with the reason for each

1. **`playwright`'s actual MCP server version.** The catalog entry and its
   `plugin.json` carry no version, and the underlying Microsoft server is pulled
   at runtime. Establishing it would require reading the plugin's MCP config and
   resolving an npm package — not done, because nothing on this axis depends on
   the number today.
2. **`frontend-design`, `skill-creator`, `playwright` "current version".** They
   publish none. What is recorded instead is the last-commit date of each
   directory, which is verifiable. Any future claim of a version number for these
   three would be invented.
3. **Whether `frontend-design`'s marketplace description or its `plugin.json`
   description is the one loaded per session.** The two differ (145 chars vs 45).
   Audit-02's cost arithmetic assumed one of them; which one the harness charges
   for was not established — it would need an install, which is out of scope.
4. **The date `anthropics/claude-plugins-public` was renamed to `-official`.**
   GitHub's API exposes the redirect but not its date, and no announcement was
   located. Only the current state is verified.
5. **Whether the 287 → 285 delta is genuinely +2 net.** Audit-02's 285 was read
   from a different phrasing ("Nothing in 285 plugins") on 2026-08-10 and may have
   counted a different field. The count of 287 today is verified; the delta is
   indicative only.
6. **`skill-creator`'s bundled contents today** (3 subagents, 8 scripts, eval
   viewer, per `notes/research-skills/skill-creator.md:28`). Not re-listed this
   run — the item is NOT MET, so the inventory changes nothing. Re-verify only if
   the trigger fires.
7. **Whether `webapp-testing` is reachable via any official-marketplace bundle.**
   Verified absent as a standalone name and absent from every entry's text; a
   bundle that ships it without naming it in its description would not be caught
   by that search.
8. **Anything about the user's other projects** — C1, C2 and C3 in full, and
   C4's deciding half. Not verifiable from this repository by construction; the
   questions are written above.
9. **Whether the harness's live-read of the kit source is documented anywhere.**
   Carried unchanged from `LAST-CHECK.md:96-98`; no Anthropic page describing it
   was searched for this run.

---

## Re-check recipes used this run — for the next run

| Source | Recipe that worked on 2026-08-11 |
|---|---|
| `claude plugin list --json` | Run from the repo root. Confirm `scope`, `enabled`, `installPath`, `installedAt`, `lastUpdated`, `errors` still present. **Expect `version: 0.1.0`** — the install record is pinned; never compare it to `plugin.json`. |
| `claude plugin details my-method` | Run it. Read the **Component inventory** line — 6 skills / 1 agent / 1 hook — and the version on line 1 (**0.2.0**). This, not `plugin list`, is the live version. |
| `claude plugin validate ./kit/my-method` | Expect `✔ Validation passed`. If it fails with "não é reconhecido", retry once — the CLI may be mid-self-update (`CHANGELOG.md:57-62`). |
| Frozen-cache check | `diff -rq "$HOME/.claude/plugins/cache/jeferson-tools/my-method/<installed-version>" kit/my-method`. Components present only in `kit/` but reported by `plugin details` = the live-read behaviour still holds. The day that diff goes quiet **or** `plugin details` starts matching the cache, the undocumented behaviour changed — that is the alarm. |
| Official marketplace catalog | `curl` the raw URL to a file and **parse it with a JSON parser**. Read `len(d['plugins'])` and `d['renames']`. **Do not use a summarizing fetch** — one returned 221 plugins and three false "does not exist" verdicts against the same file the same day. |
| A plugin's version in that catalog | Look for `version` in the catalog entry; if absent, fetch `.../main/<source-path>/.claude-plugin/plugin.json`. If still absent, **there is no version** — 273 of 287 entries have none. Use the directory's last-commit date instead: `GET https://api.github.com/repos/anthropics/claude-plugins-official/commits?path=<dir>&per_page=2`. |
| `webapp-testing` (skills repo) | `GET https://api.github.com/repos/anthropics/skills/contents/skills/webapp-testing` — expect `SKILL.md`, `LICENSE.txt`, `examples/`, `scripts/`. Then read the `name:` and `description:` in the SKILL.md frontmatter. Check `archived` on `GET .../repos/anthropics/skills`. |
| Any `anthropics/*` repo URL in a recipe | `curl -sS -D -` and look for **301**. `claude-plugins-public` → `claude-plugins-official` is a live example; a 200 through a redirect hides a rename. |
| The six triggers, in-repo | `find . -iname "*PLAN*"` (a product plan would be new); read `friction.md`'s **`MINE`** section (empty today — any entry is news); read the newest `CHANGELOG.md` entries down to the last-check date; `grep -i "tier"` the newest pilot record for anything other than **T0**. |
