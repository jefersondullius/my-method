# Axis A — Anthropic practices — maintenance run 2026-08-11

Scope: every row under "Axis A" in `notes/maintenance/WATCHLIST.md`.
All sources **accessed 2026-08-11**. Every claim carries a URL. Anything not
confirmed from a live source is in **NOT VERIFIED** with its reason.

**Window.** The prior run (`notes/research-maintenance/cli-mechanics.md`) verified the
CLI/plugin mechanics and read the changelog only far enough to establish 2.1.227
(2026-08-10). For every docs page that file did not cover, the lower bound is
2026-08-10, this repository's first commit. Items that file left NOT VERIFIED were
re-checked here and are marked.

**Environment probe:** `claude --version` → `2.1.228 (Claude Code)`.

**Method used on every docs page:** fetched the `.md` variant (`<url>.md`), which returns
clean markdown, then read the specific table row or section rather than trusting a
summary. Where a page exceeded the fetch limit it was persisted to disk and grepped for
the exact field name. No page in this axis was a redirect shell, a meta-refresh stub or
an empty JS page.

---

## Row 1 — Docs changelog

- **URL:** <https://code.claude.com/docs/en/changelog.md>
- **Accessed:** 2026-08-11
- **Re-check recipe used:** fetched the `.md` variant and read the first version heading.
  Then re-fetched with a cache-busting query (`?v=recheck`) and asked specifically
  whether a `2.1.228` entry exists anywhere on the page.
- **What I read:** the first eight version entries in full. The first version entry is
  verbatim:

  > ## 2.1.227 (August 10, 2026)

  Its five bullets concern feature-flag evaluation with an expired login token,
  `claude-code-action` with `allowed_non_write_users`, `/tui` rewind, the slash-command
  menu colouring, and event-loop stalls. Entries below it: 2.1.226 (August 8), 2.1.225
  (August 8), 2.1.224 (August 7), 2.1.223 (August 6), 2.1.222 (August 4), 2.1.221
  (August 4).

- **Verdict: UNCHANGED.** Same latest version and date the prior run recorded.

- **New fact worth recording:** the installed CLI is **2.1.228**, and **there is no
  2.1.228 entry anywhere on the changelog page.** The published changelog lags the
  shipped binary by at least one version. Any recipe that reads the changelog's top
  heading as "the current CLI version" is reading the wrong thing — it is the latest
  *documented* version.

- **Note on the recipe wording:** the row says "read the first `##` heading". On the
  `.md` variant the literal first `##` heading is `## Documentation Index` (a fetch
  preamble, not content). The first *version* heading is what the row means. Worth
  tightening so a future run does not report "Documentation Index" as the version.

---

## Row 2 — Hooks

- **URL:** <https://code.claude.com/docs/en/hooks> (read as `hooks.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `PreToolUse` still lists "Can block it", and that
  `deny` is still a documented `permissionDecision` value. Read the lifecycle table row,
  the exit-code-2 table row, and the decision-control enumeration.
- **What I read**, verbatim:

  Lifecycle table:

  > | `PreToolUse` | Before a tool call executes. Can block it |

  Exit-code table:

  > | `PreToolUse` | Yes | Blocks the tool call |

  Decision-control enumeration:

  > `permissionDecision`: one of `"allow"`, `"deny"`, `"ask"`, `"defer"`

  And the documented output shape, unchanged from the prior run's record:

  ```json
  {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "Destructive command blocked by hook"
    }
  }
  ```

  Plugin hook location row:

  > | Plugin `hooks/hooks.json` | When plugin is enabled | Yes, bundled with the plugin |

  `disableAllHooks`, verbatim:

  > To temporarily disable all hooks without removing them, set `"disableAllHooks": true`
  > in your settings file. There is no way to disable an individual hook while keeping it
  > in the configuration.

  > The `disableAllHooks` setting respects the managed settings hierarchy. If an
  > administrator has configured hooks through managed policy settings, `disableAllHooks`
  > set in user, project, or local settings can't disable those managed hooks. Only
  > `disableAllHooks` set at the managed settings level can disable managed hooks.

- **Verdict: UNCHANGED.** Every mechanism the commit gate depends on is intact.

---

## Row 3 — Skills

- **URL:** <https://code.claude.com/docs/en/skills> (read as `skills.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `disable-model-invocation` and `argument-hint` still
  exist in the frontmatter reference table. Page exceeded the fetch limit, so it was
  persisted and the frontmatter table read directly by line.
- **What I read**, verbatim from the frontmatter reference table:

  > | `argument-hint` | No | Hint shown during autocomplete to indicate expected
  > arguments. Example: `[issue-number]` or `[filename] [format]`. |

  > | `disable-model-invocation` | No | Set to `true` to prevent Claude from automatically
  > loading this skill. Use for workflows you want to trigger manually with `/name`. Also
  > prevents the skill from being preloaded into subagents. As of v2.1.196, also prevents
  > the skill from running when a scheduled task fires with the skill as its prompt.
  > Default: `false`. |

  Both rows are **character-for-character identical** to what the prior run recorded.
  The surrounding table still carries `name`, `description`, `when_to_use`, `arguments`,
  `user-invocable`, `allowed-tools`, `disallowed-tools`, `model`, `effort`, `context`,
  `agent`, `background`, `hooks`, `paths`, `shell`, `metadata`, `license`,
  `compatibility`.

  Also still present, and still relevant to this kit:

  > | `${CLAUDE_EFFORT}` | The current effort level: `low`, `medium`, `high`, `xhigh`, or
  > `max`. Ultracode is not a distinct level and reports as `xhigh`. […] |

  > The `user-invocable` field only controls menu visibility, not Skill tool access. Use
  > `disable-model-invocation: true` to block programmatic invocation.

- **Verdict: UNCHANGED.** All five commands that carry
  `disable-model-invocation: true` and the two that carry `argument-hint` remain valid.

---

## Row 4 — Settings

- **URL:** <https://code.claude.com/docs/en/settings> (read as `settings.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `effortLevel` accepts the same values and
  `disableAllHooks` still exists. "The same values" was resolved against the recorded
  baseline in `notes/research-maintenance/sources-baseline.md` line 473, which reads
  "Accepts `low` | `medium` | `high` | `xhigh` only."
- **What I read**, verbatim:

  > | `effortLevel` | Persist the effort level across sessions. Accepts `"low"`,
  > `"medium"`, `"high"`, or `"xhigh"`. Written automatically when you run `/effort` with
  > one of those values. `--effort` and `CLAUDE_CODE_EFFORT_LEVEL` override this for one
  > session. […] | `"xhigh"` |

  > | `disableAllHooks` | Disable all hooks and any custom status line | `true` |

  Cross-checked against <https://code.claude.com/docs/en/model-config.md> (accessed
  2026-08-11), which agrees:

  > * **Settings**: set `effortLevel` to `low`, `medium`, `high`, or `xhigh` in your
  >   settings file. `max` and `ultracode` are session-only and are not accepted here

  The per-model effort table on that page also still matches the recorded baseline
  (`sources-baseline.md` lines 444-446) exactly:

  > | Fable 5 | `low`, `medium`, `high`, `xhigh`, `max` |
  > | Opus 5, Sonnet 5, Opus 4.8, and Opus 4.7 | `low`, `medium`, `high`, `xhigh`, `max` |
  > | Opus 4.6 and Sonnet 4.6 | `low`, `medium`, `high`, `max` |

- **Verdict: UNCHANGED.** Both keys present, `effortLevel`'s accepted values identical to
  the baseline, and the gate's escape hatch (`disableAllHooks`) intact — including the
  managed-settings caveat that `verify-gate.ps1` line 72 tells the user about.

---

## Row 5 — Built-in commands

- **URL:** <https://code.claude.com/docs/en/commands> (read as `commands.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `/model` is still described as user-only. Because
  this is load-bearing for `method.md` STEP 5a, I did not accept a summary: I pulled the
  entire page (122KB, persisted) and grepped it for every user-only phrasing
  (`only you`, `user-only`, `Claude can't`, `cannot`, `invoked by Claude`, `Availability`)
  and then read the `/model` and `/effort` table rows in full.

- **What I found. There is no sentence on this page saying `/model` is user-only, and
  there does not appear ever to have been one.** The repository's own earlier note is
  explicit that this was an inference, not a quote — `notes/audit-01-gaps.md` line 12
  records `/clear`, `/model`, `/effort` as "built-in commands, 'recognized at the start
  of your message' (i.e., typed by the user)". The parenthetical is the repo's reasoning,
  not the docs' wording.

- **The two sentences the claim actually rests on are both still present.** Verbatim:

  > A command is only recognized at the start of your message.

  > The table below lists all the commands included in Claude Code. Most are built-in
  > commands whose behavior is coded into the CLI. Two kinds of entries are marked:
  >
  > * **[Skill]**: a bundled skill. It works like skills you write yourself: a prompt
  >   handed to Claude, which Claude can also invoke automatically when relevant.
  >   * `/verify` and `/code-review` run only when you invoke them. Before v2.1.215,
  >     Claude could also run them on its own.
  > * **[Workflow]**: a bundled dynamic workflow that fans work out across many subagents
  >   and runs in the background.

  This is **stronger** evidence than a bare assertion would be, and it is structural. The
  page marks exactly which entries Claude can invoke on its own: the 14 rows tagged
  `[Skill]`. I checked the `/model` row (line 102 of the raw page) and the `/effort` row
  (line 76): **neither carries a `[Skill]` or `[Workflow]` marker.** They are built-ins
  "whose behavior is coded into the CLI" — not prompts handed to Claude — so Claude has
  no documented path to invoke them.

  The `/model` row verbatim, in full:

  > | `/model [model]` | Switch the AI model and save it as your default for new sessions.
  > For models that support it, use left/right arrows to adjust effort level. With no
  > argument, opens a picker; press `s` on a row to switch for the current session only.
  > The picker asks for confirmation when the conversation has prior output, since the
  > next response re-reads the full history without cached context. Once confirmed, the
  > change applies without waiting for the current response to finish. Also available in
  > non-interactive mode (`-p`) with a model argument instead of the picker, where it
  > applies to the current session only and isn't saved as your default; requires Claude
  > Code v2.1.205 or later |

  The `-p` clause is the **user** passing a value on the command line, not Claude
  switching itself.

- **Verdict: UNCHANGED (the substance holds) — but the RECIPE IS WRONG and should be
  rewritten.** "Confirm `/model` is still described as user-only" instructs a future run
  to look for wording that does not exist, which invites either a false CHANGED verdict
  or a shrug. The recipe should instead be: *confirm the `/model` row still carries no
  `[Skill]`/`[Workflow]` marker, and that the legend above the table still says built-ins
  are "coded into the CLI" while only `[Skill]` entries are "a prompt handed to Claude,
  which Claude can also invoke automatically".*

- **One qualification that does touch the method.** `model-config.md` (accessed
  2026-08-11) lists six ways to change effort, and the sixth is not the user:

  > * **Skill and subagent frontmatter**: set `effort` in a skill or subagent markdown
  >   file to override the effort level when that skill or subagent runs

  So "only the user can switch model or effort" is true of the *interactive commands*,
  but a skill or agent file can set model/effort for its own turn. `method.md` already
  chose not to use that path deliberately (it is turn-scoped), so the design is
  unaffected — but the sentence as written in STEP 5a is broader than the docs support.

---

## Row 6 — Plugins reference

- **URL:** <https://code.claude.com/docs/en/plugins-reference> (read as
  `plugins-reference.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** read the "Plugin caching and file resolution" section — it is
  what D-VER guards against. Page exceeded the fetch limit; persisted and read the whole
  section by line, plus the symlink and path-traversal subsections.
- **What I read.** The load-bearing claim is intact, verbatim:

  > For security and verification purposes, Claude Code copies *marketplace* plugins to
  > the user's local **plugin cache** (`~/.claude/plugins/cache`) rather than using them
  > in-place.

  > ### Path traversal limitations
  >
  > Installed plugins cannot reference files outside their directory. Paths that traverse
  > outside the plugin root (such as `../shared-utils`) will not work after installation
  > because those external files are not copied to the cache.

  > Unlike a marketplace install, the plugin is discovered in place rather than copied
  > into the plugin cache. *(skills-directory plugins)*

- **Verdict: CHANGED — additive only. The guarantee D-VER relies on is untouched.**
  Four differences against what the prior run recorded:

  1. **Cache directory naming is now specified.** Prior run: "Each installed version is a
     separate directory in the cache." Today, verbatim:
     > Each installed version is a separate directory in the cache, grouped by marketplace
     > and plugin and named for the resolved version, with its own copy of the plugin's
     > files and Node.js package dependencies. A dependency resolved from a release tag
     > gets a directory name with a commit-SHA suffix.

     This documents the exact layout the CLI probe below shows
     (`…\cache\jeferson-tools\my-method\0.1.0`).

  2. **The 14-day orphan sweep gained a condition.** Verbatim:
     > Claude Code runs the sweep only while at least one plugin is installed; after you
     > uninstall your last plugin, orphaned directories stay on disk until you install a
     > plugin again.

     Plus a new sentence: "Claude's Glob and Grep tools skip orphaned version directories
     during searches, so file results don't include outdated plugin code."

  3. **A whole new subsection, "Node.js package dependencies."** Claude Code now runs
     `npm ci --ignore-scripts` or `bun install --frozen-lockfile --ignore-scripts` inside
     the cached version directory when the plugin root has both a `package.json` and a
     supported lockfile — at install, at update, and at session start when an enabled
     plugin isn't cached yet. `yarn.lock` and `pnpm-lock.yaml` are deliberately skipped.
     60-second timeout. Cannot be disabled.

  4. **Symlink handling is now a three-case rule** rather than one sentence: preserved
     within the plugin's own directory, dereferenced elsewhere in the same marketplace,
     skipped outside the marketplace. The sentence the prior run recorded is still there
     verbatim: "For plugins installed with `--plugin-dir` or from a local path, only
     symlinks that resolve within the plugin's own directory are preserved. All others are
     skipped."

  Whether items 2-4 are new *since 2026-08-10* or merely unquoted by the prior run is
  **NOT VERIFIED** (see NOT VERIFIED #2) — the prior run quoted the section selectively.

---

## Row 7 — Plugin marketplaces

- **URL:** <https://code.claude.com/docs/en/plugin-marketplaces> (read as
  `plugin-marketplaces.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm local-path marketplace sources are still supported.
  Persisted the page and grepped the source-type table plus the local-marketplace
  walkthrough.
- **What I read**, verbatim from the plugin-source table:

  > | Relative path | `string` (e.g. `"./my-plugin"`) | none | Local directory within the
  > marketplace repo. Must start with `./`. Resolved relative to the marketplace root, not
  > the `.claude-plugin/` directory |

  The overview sentence still names local paths as first-class:

  > Marketplaces provide centralized discovery, version tracking, automatic updates, and
  > support for multiple source types, including git repositories and local paths.

  The "Walkthrough: create a local marketplace" section is still present, and still uses
  `"source": "./plugins/quality-review-plugin"` — the exact shape this repo uses.

- **Verdict: UNCHANGED.** This repo remains a supported marketplace shape.

- **Two additions noted, neither breaking.** A new `archive` source type (install from a
  zip over HTTPS with optional SHA-256 pinning — this matches changelog 2.1.224), and a
  caveat that is worth knowing but does not apply here, verbatim:
  > Claude Code resolves relative paths against a local copy of the marketplace, so they
  > work when users add your marketplace from a git source or a local directory. If users
  > add your marketplace via a direct URL to the `marketplace.json` file, relative paths
  > won't resolve, because Claude Code downloads only that file.

  This repo is added as a local directory, so relative paths resolve.

---

## Row 8 — Subagents

- **URL:** <https://code.claude.com/docs/en/sub-agents> (read as `sub-agents.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `tools` is still an allowlist and plugin agents still
  honour it. Persisted the page and read the frontmatter table plus every sentence
  mentioning plugin subagents.
- **What I read**, verbatim:

  > | `tools` | No | Tools the subagent can use. Inherits every tool available to
  > subagents if omitted. If no entry in the list resolves to a tool, the subagent usually
  > fails to launch with an error naming the entries. To preload Skills into context, use
  > the `skills` field rather than listing `Skill` here |

  > To restrict tools, use the `tools` field as an allowlist or the `disallowedTools`
  > field as a denylist. This example uses `tools` to allow only Read, Grep, Glob, and
  > Bash. The subagent can't edit files, write files, or use any MCP tools

  On plugin agents honouring it — the exclusion list is closed and `tools` is not in it:

  > For security reasons, plugin subagents don't support the `hooks`, `mcpServers`, or
  > `permissionMode` frontmatter fields. These fields are ignored when loading agents from
  > a plugin. If you need them, copy the agent file into `.claude/agents/` or
  > `~/.claude/agents/`.

  The three "Ignored for plugin subagents" annotations in the frontmatter table appear on
  exactly `permissionMode`, `mcpServers` and `hooks` — **not** on `tools`.

  Override precedence, still documented:

  > Project and user `.claude/agents/` definitions override same-named plugin agents.

- **Verdict: UNCHANGED.** `security-reviewer`'s `tools: Read, Grep, Glob` allowlist is
  still structurally enforced, and still enforced for a plugin-shipped agent.

---

## Row 9 — Headless mode

- **URL:** <https://code.claude.com/docs/en/headless> (read as `headless.md`)
- **Accessed:** 2026-08-11
- **Re-check recipe used:** confirm `-p` still loads plugins by default and only `--bare`
  skips them. Read the full "Start faster with bare mode" section and the auto-approve
  note.
- **What I read**, verbatim:

  > Add `--bare` to reduce startup time by skipping auto-discovery of hooks, skills,
  > plugins, MCP servers, auto memory, and CLAUDE.md. Without it, `claude -p` loads the
  > same context an interactive session would, including anything configured in the
  > working directory or `~/.claude`.

  And the skills-in-`-p` note, unchanged:

  > User-invoked skills and custom commands work in `-p` mode: include `/skill-name` in
  > the prompt string and Claude Code expands it before running.

- **Verdict: UNCHANGED — with one live warning that the prior run did not record.**
  A `<Note>` on the same page reads, verbatim:

  > `--bare` is the recommended mode for scripted and SDK calls, and will become the
  > default for `-p` in a future release.

  If that lands, `claude -p` stops loading plugins by default, and every probe in this
  repo that relies on the kit being loaded in a `-p` run silently stops testing the kit.
  I could not date this sentence: a search of the whole changelog for `--bare` / "bare
  mode" returns **no entry at any version**, so there is no way to tell from the docs
  whether it is new since 2026-08-10 (NOT VERIFIED #3).

---

## Rows 10-12 — CLI probes (run from `C:\Users\Jeferson\Documents\dev\my-method`)

### Row 10 — `claude plugin list --json`

- **Recipe used:** ran it; checked that `scope`, `enabled`, `installPath`, `errors` are
  in the real output.
- **Raw output:**

  ```json
  [
    {
      "id": "my-method@jeferson-tools",
      "version": "0.1.0",
      "scope": "user",
      "enabled": true,
      "installPath": "C:\\Users\\Jeferson\\.claude\\plugins\\cache\\jeferson-tools\\my-method\\0.1.0",
      "installedAt": "2026-08-10T05:17:26.209Z",
      "lastUpdated": "2026-08-10T05:17:26.209Z"
    },
    {
      "id": "my-method@skills-dir",
      "version": "unknown",
      "scope": "user",
      "enabled": false,
      "installPath": "",
      "errors": [
        "Not loaded — the name \"my-method\" is already taken by an installed plugin (my-method@jeferson-tools), which takes precedence. Give the plugin at .\\.claude\\skills\\my-method a different \"name\" (in plugin.json or SKILL.md frontmatter) to load this copy."
      ]
    }
  ]
  ```

- **Verdict: UNCHANGED / CONFIRMED.** All four fields the row names are present.
  `installedAt` and `lastUpdated` are also emitted — still more than the docs describe
  (the docs still say only "version, source marketplace, and enable status"). This also
  settles the prior run's **NOT VERIFIED #1**: `plugin list --json` **does** carry both
  install scope and install path, empirically, even though the docs do not say so. And it
  settles **NOT VERIFIED #5**: a plugin that failed to load **is** listed, with an
  `errors` array — the `my-method@skills-dir` entry is the proof.

- **The `my-method@skills-dir` entry is expected.** `kit/my-method/commands/health-check.md`
  lines 24-27 already predict it by name and call it harmless. Its path
  (`.\.claude\skills\my-method`) is not in this repository — `.claude/skills/` does not
  exist here — so it lives under the user's home directory, which I did not read
  (boundary rule).

### Row 11 — `claude plugin details my-method`

- **Recipe used:** ran it; confirmed the Skills/Agents/Hooks grouping still prints.
- **Raw output:**

  ```
  my-method 0.2.0
    Executable form of method.md: the working method for building products with Claude doing the engineering.
    Source: my-method@jeferson-tools

  Component inventory
    Skills (6)  friction, health-check, next-task, start-project, update-method, where-am-i
    Agents (1)  security-reviewer
    Hooks (1)  PreToolUse  (harness-only — no model context cost)
    MCP servers (0)
    LSP servers (0)

  Projected token cost
    Always-on:   ~457 tok   added to every session

  Per-component (rounded)
    component          always-on  on-invoke
    security-reviewer        ~70       ~790
    friction                 ~40       ~740
    health-check             ~70        ~2k
    next-task                ~50      ~2.6k
    start-project            ~60     ~18.1k
    update-method           ~110        ~4k
    where-am-i               ~50       ~610
  ```

- **Verdict: UNCHANGED / CONFIRMED.** The Skills/Agents/Hooks grouping prints, and the
  inventory matches the kit on disk exactly: 6 command files, 1 agent, 1 `PreToolUse`
  hook entry.

### Row 12 — `claude plugin validate ./kit/my-method`

- **Recipe used:** ran it; expected `✔ Validation passed`.
- **Raw output:**

  ```
  Validating plugin manifest: C:\Users\Jeferson\Documents\dev\my-method\kit\my-method\.claude-plugin\plugin.json

  ✔ Validation passed
  ```

- **Verdict: UNCHANGED / CONFIRMED.** Exactly the documented success string, with no
  warnings. (Changelog 2.1.221 added marketplace/plugin-name warnings for Claude Desktop
  managed sync; none fired here.)

---

# CHANGES THAT TOUCH THIS REPO

**1. `claude -p` will stop loading plugins by default.**
Source: <https://code.claude.com/docs/en/headless> (accessed 2026-08-11) — "`--bare` […]
will become the default for `-p` in a future release."
**Touches: the plugin AND the method.** Named targets:
- `kit/my-method/commands/health-check.md`, **Probe 1 step 1** — "This command executing
  at all proves the plugin loaded in this session." In a future `-p` run under a default
  `--bare`, the command would not execute at all, so the probe cannot report FAILED; it
  simply never runs. Probe 1 needs a note that a `-p` health-check must pass an explicit
  `--plugin-dir`, or must not be run under `-p` at all.
- `kit/my-method/commands/health-check.md`, **Probe 3** (commit gate) — same exposure:
  bare mode skips hook auto-discovery, so the gate would not fire and the probe would
  read "gate did not fire" as a pass condition it was never testing.
- `kit/my-method/commands/update-method.md`, **Axis E sweep** — this is exactly the kind
  of scheduled breaking change the sweep exists to catch; it should be recorded now, with
  its trigger being "a Claude Code release note announcing bare-by-default".
- `notes/maintenance/WATCHLIST.md`, **Axis A row 9** — the recipe currently reads
  "Confirm `-p` still loads plugins by default (only `--bare` skips them)". It should
  additionally read the `<Note>` under "Start faster with bare mode", because that Note
  is where the reversal will be announced.
Not yet actionable — no version is named. Record and watch.

**2. Watchlist Axis A row 5's recipe asks for wording that does not exist.**
Source: <https://code.claude.com/docs/en/commands> (accessed 2026-08-11).
**Touches: the method AND the watchlist.** Named targets:
- `notes/maintenance/WATCHLIST.md`, **Axis A row 5** and **Axis D row 1** — both say
  "confirm `/model` is still described as user-only". Nothing on that page describes it
  that way. Replace with the structural check: the `/model` row carries no `[Skill]` or
  `[Workflow]` marker, and the legend still says built-ins are "coded into the CLI" while
  `[Skill]` entries are "a prompt handed to Claude, which Claude can also invoke
  automatically when relevant".
- `kit/my-method/commands/update-method.md` **line 87** — "is `/model` still user-only, is
  `/effort` still user-only" sends a future run looking for the same absent sentence.
  Same fix.
The design itself is safe: the structural evidence is stronger than the wording the
watchlist was hunting for.

**3. `method.md` STEP 5a overstates "only the user can switch model or effort".**
Source: <https://code.claude.com/docs/en/model-config> (accessed 2026-08-11), the "Set the
effort level" list, sixth bullet — skill and subagent frontmatter can set `effort`.
**Touches: the method.** Named targets:
- `method.md`, **STEP 5a**, the sentence "Only the user can switch model or effort".
- `kit/my-method/commands/start-project.md` **line 171** and
  `kit/my-method/commands/next-task.md` **lines 46-47**, which carry the same instruction.
True for the `/model` and `/effort` commands; not true in general, because a skill or
agent file can set `effort:` (and `model:`) for its own turn. v7 deliberately rejected
that path as turn-scoped, so no behaviour changes — but the claim should be narrowed to
"only the user can change the *session's* model or effort" so a future reader does not
find the docs contradicting the method.

**4. The installed plugin is pinned at 0.1.0 while the kit on disk is 0.2.0.**
Source: the two CLI probes above (run 2026-08-11), plus
`kit/my-method/.claude-plugin/plugin.json` which reads `"version": "0.2.0"`.
`plugin list --json` reports `"version": "0.1.0"` with
`installPath …\cache\jeferson-tools\my-method\0.1.0` and `installedAt 2026-08-10`, while
`plugin details my-method` reports `0.2.0`.
**Touches: the plugin.** Named targets:
- `kit/my-method/commands/update-method.md` **lines 189-191** already predicts precisely
  this ("the install record stays pinned: `plugin list` … while `plugin details` reports
  the source. A difference …"). Confirmed correct in the field — that note is doing its
  job.
- `kit/my-method/commands/health-check.md`, **Probe 1 step 3** — this is the gap.
  Probe 1 step 3 compares the *component inventory* (6 skills / 1 agent / 1 hook), and
  the inventory **matches**, so the probe passes. Only the version number diverges. A
  stale install with an unchanged component count is invisible to the current check.
  Probe 1 step 3 should also compare `plugin list --json`'s `version` against
  `kit/my-method/.claude-plugin/plugin.json`'s `version`.
This is corroborated by the docs (Row 6): marketplace plugins are copied to
`~/.claude/plugins/cache` and each version is its own directory, so a 0.1.0 install keeps
loading until updated.

**5. Plugin cache now runs an automatic Node.js dependency install.**
Source: <https://code.claude.com/docs/en/plugins-reference> (accessed 2026-08-11), new
"Node.js package dependencies" subsection. Runs `npm ci --ignore-scripts` (or Bun) inside
the cached version directory whenever the plugin root has both a `package.json` and a
supported lockfile — at install, at update, and at session start on a new machine. Cannot
be disabled.
**Touches: the plugin.** Named target: `kit/my-method/` has no `package.json`, so nothing
runs today. This matters only as a constraint on any future kit change that adds one —
`yarn.lock` and `pnpm-lock.yaml` are skipped, and a 60-second timeout can leave a partial
`node_modules` in the cache. Worth one line in the watchlist's Row 6 recipe so it is not
rediscovered later.

---

# CHANGES THAT TOUCH NEITHER

- `enabledPlugins` is no longer a row in the settings table; it survives only inside the
  `allowManagedHooksOnly` description. `discover-plugins` still links to
  `/docs/en/settings#enabledplugins`, so that cross-reference is now stale. The kit uses
  no such setting.
- New settings keys visible in the plugin area: `allowedChannelPlugins`,
  `blockedMarketplaces`, `strictKnownMarketplaces` (the last gained `"owner/*"` wildcards
  in 2.1.223). Managed-settings territory; this repo sets none of them.
- New `archive` plugin source type (zip over HTTPS, optional SHA-256 pinning), changelog
  2.1.224. This repo is a local-path marketplace.
- `permissionDecision` still documents a fourth value, `"defer"`, alongside
  `allow`/`deny`/`ask`. The gate only ever emits `deny`.
- Changelog 2.1.223: `/review` is now an alias of `/code-review`. The kit invokes neither.
- Changelog 2.1.222: "Removed ultraplan feature". Not referenced anywhere in this repo.
- Changelog 2.1.224: 200-subagent-per-session spawn cap removed. The kit spawns one.
- Changelog 2.1.222: subagent transcript spinner now shows the subagent's own `effort:`.
  Cosmetic.
- Changelog 2.1.221: `claude plugin validate` gained Claude-Desktop-sync name warnings.
  None fired for this kit.
- Changelog 2.1.221: plugins installed from `/plugin` now activate immediately when safe
  instead of always needing `/reload-plugins`. Does not change the kit's apply steps,
  which are explicit anyway.
- Changelog 2.1.222: "Improved the refusal when Claude tries to invoke a skill with
  `disable-model-invocation`" — behaviour around a field the kit uses, but it only changes
  what Claude is told on refusal, not whether the field works.
- Skills frontmatter gained `background` (v2.1.218+) and boolean parsing widened to
  `yes/no/on/off/1/0` (v2.1.218). The kit uses `true` only.

---

# NOT VERIFIED

1. **Whether the installed 0.1.0 cache copy differs in content from `kit/my-method/` at
   0.2.0.** Confirming it would require reading
   `C:\Users\Jeferson\.claude\plugins\cache\jeferson-tools\my-method\0.1.0`, which is
   outside this project folder. My standing boundary rule forbids reading there, so this
   was not checked. The version and path divergence is recorded from CLI output only.

2. **Whether the four additions in the "Plugin caching and file resolution" section
   (Row 6) are new since 2026-08-10, or were simply not quoted by the prior run.**
   `notes/research-maintenance/cli-mechanics.md` §5 quoted that section selectively, so
   absence from that file is not evidence of absence from the page on 2026-08-10. No
   changelog entry names the section. Recorded as "present today", not as "added".

3. **The date the sentence "`--bare` […] will become the default for `-p`" appeared.**
   Searched the entire changelog for `--bare` and "bare mode": no entry at any version
   mentions either. The prior run quoted the surrounding `--bare` paragraphs but not this
   `<Note>`, so I cannot tell whether it is new. The statement is live on the page as of
   2026-08-11; its age is unknown.

4. **Whether 2.1.228 (the installed CLI version) contains anything relevant.** There is no
   2.1.228 entry on the changelog page as of 2026-08-11. Its contents cannot be read from
   the documentation. Re-check on the next run.

5. **Where `.claude/skills/my-method` actually lives** (the source of the
   `my-method@skills-dir` error). It is not in this repository — `.claude/skills/` does
   not exist here — so it is under the user's home directory, which I did not read
   (boundary rule). Reported as the CLI reports it.

6. **Whether `/hooks`, `/plugin`, `/skills` or `/context` produce output in `-p` mode**
   (prior run's NOT VERIFIED #3). Still not stated in the docs; the `-p` degradation list
   on the headless page still names only `/model`, `/effort`, `/fast`, `/color`,
   `/rename`, `/mcp` and `/config`. Unchanged and still unverified.

7. **The command-markdown `${CLAUDE_PLUGIN_ROOT}` dispute** (prior run's NOT VERIFIED #7).
   Out of scope for this pass: it is an issue-tracker question, not a docs-page question,
   and no Axis A row covers it. The docs position is unchanged — the plugins-reference
   substitution table still says the placeholder resolves "anywhere the placeholder
   appears" in skill and agent content. `hooks/hooks.json` uses it in a hook command,
   which is the one context nobody disputes.
